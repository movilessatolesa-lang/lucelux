# Jona_251147854

This workspace contains a Next.js application scaffolded to match `create-next-app` defaults as closely as possible for TypeScript, Tailwind CSS, ESLint, and the App Router.

## Available scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run lint`

## Notes

- The automatic scaffold command could not be executed in this VS Code session because no terminal was available to the task runner.
- The project files have been created directly so the app can be installed and started once terminal access is available.
